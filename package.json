{
  "name": "test-imap-simple",
  "version": "1.0.0",
  "description": "save your account info in `config.json` as `config.json.skel`",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "imap-simple": "^5.1.0"
  }
}
